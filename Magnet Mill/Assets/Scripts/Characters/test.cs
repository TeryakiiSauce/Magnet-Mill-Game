using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class thtr : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Hello, World!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
